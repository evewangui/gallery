<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="css/styles.css">
  <title>Darkroom</title>
</head>

<body style="background-color: #f4f4f4;">
  <div class="navbar-fixed">
    <nav class="turquoise">
      <div class="nav-wrapper container">
        <a href="/" class="brand-logo">Darkroom</a>
        <form class="right">
          <div class="input-field">
            <input id="search" class="left" type="search" required>
            <label class="label-icon" for="search"><i class="material-icons right">search</i></label>
            <i class="material-icons">close</i>
          </div>
        </form>
      </div>
    </nav>
  </div>

  <div class="container center-align" style="margin-top: 2rem;">
    <!--  MILESTONE 2 Banner -->
    <div class="card-panel teal lighten-2 white-text z-depth-2">
      <h4><i class="material-icons left">flag</i> MILESTONE 3</h4>
      <p>Continuous Integration and Deployment successfully configured 🎉</p>
    </div>

    <h1>Image Upload</h1>

    <form action="/upload" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="image">Select Image:</label>
        <input type="file" name="image" id="image" accept="image/*" required>
      </div>
      <button type="submit" class="btn btn-primary">Upload</button>
    </form>

    <% if (locals.msg) { %>
    <div class="alert alert-info">
      <%= msg %>
    </div>
    <% } %>

    <hr>
    <br>

    <div class="row">
      <% images.forEach(function(image){ %>
      <div class="column">
        <a href="/image/<%= image._id %>">
          <img src="<%= image.path %>" class="responsive-img" alt="<%= image.name %>">
        </a>
      </div>
      <% }); %>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>